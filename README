# NVIDIA GPU 内存拷贝测试结果

## 测试变量
1. 单次操作数据大小：1, 2, 4, 8, 16, 32 字节
2. 单线程通过for循环处理多个数据

## 测试结果
在内存大小为2GB的情况下：
- 当单次操作数据大小大于4字节时，且每个线程处理8个数据以下的时候，性能表现相近
- 当unit=32bytes时，编译成ptx码会分成两个16byte的操作，可能与使用的4060ti显卡有关

## 关键发现
